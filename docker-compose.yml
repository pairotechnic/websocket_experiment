services:
  tictactoe:
    image: ghcr.io/${GITHUB_REPOSITORY}:sha-${IMAGE_TAG}
    container_name: tictactoe
    ports:
      - "8000:8000"
    environment:
      - PYTHONBUFFERED=1
    restart: unless-stopped